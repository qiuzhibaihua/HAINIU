import {
    StyleSheet,
} from 'react-native';
const styles = StyleSheet.create({
    buttonCommal: {
        color:'#fff',
        fontSize:20,
        padding:10,
        width:'100%',
        textAlign:'center',
        borderRadius:20,
        overflow: 'hidden',
    },
   
});
export default styles
